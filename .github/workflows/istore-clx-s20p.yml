#
# GitHub Actions OpenWrt 编译配置文件
# 设备机型: istore-clx-s20p (MT7986)
#

name: istore-clx-s20p

env:
  # 设备名称，用于生成发布标签
  DEVICE_NAME: istore-clx-s20p
  # 源码仓库地址
  # REPO_URL: https://github.com/dailook/immortalwrt-mt798x-6.6
  REPO_URL: https://github.com/padavanonly/immortalwrt-mt798x-6.6
  # 源码分支
  REPO_BRANCH: openwrt-24.10-6.6
  # Feeds 配置文件名
  FEEDS_CONF: feeds.conf.default
  # .config 配置文件名
  CONFIG_FILE: istore/istore-clx-s20p.config
  # 自定义脚本 1 (在更新 feeds 之前运行)
  DIY_P1_SH: diy-part1.sh
  # 自定义脚本 2 (在更新 feeds 之后运行)
  DIY_P2_SH: diy-part2.sh
  # 自定义脚本 3 (在更新 feeds 之后运行)
  DIY_P3_SH: diy-part3.sh
  # 上传 bin 目录 (包含所有 ipk)
  UPLOAD_BIN_DIR: true
  # 上传固件目录 (包含 img/bin)
  UPLOAD_FIRMWARE: true
  # 上传到第三方网盘
  UPLOAD_COWTRANSFER: false
  UPLOAD_WETRANSFER: false
  # 发布到 GitHub Releases
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: '是否开启 SSH 调试'
        required: false
        default: 'false'

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout （检出仓库源码）
      uses: actions/checkout@main

    - name: Ultimate Disk Space Cleanup & Maximize Swap （磁盘清理与虚拟内存扩容）
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        # 1. 强力卸载不需要的服务和包 (可释放约 20GB+)
        echo ">>> Removing large pre-installed software..."
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /opt/ghc
        sudo rm -rf /opt/hostedtoolcache/CodeQL
        sudo rm -rf /usr/local/share/boost
        sudo rm -rf /usr/lib/jvm
        
        # 2. 清理 Docker 镜像和系统缓存
        echo ">>> Cleaning up Docker and APT..."
        sudo docker image prune --all --force
        sudo apt-get update
        sudo apt-get autoremove -y >/dev/null 2>&1
        sudo apt-get autoclean -y >/dev/null 2>&1
        
        # 3. 动态调整 Swap 空间
        # 注意：Rust 编译 LLVM 至少需要 10GB+ 的虚拟内存
        echo ">>> Reconfiguring Swap Space..."
        sudo swapoff -a
        sudo rm -f /mnt/swapfile
        # 在 /mnt 分区创建 12GB 的 Swap 文件（/mnt 空间通常比根目录充裕）
        sudo fallocate -l 12G /mnt/swapfile
        sudo chmod 600 /mnt/swapfile
        sudo mkswap /mnt/swapfile
        sudo swapon /mnt/swapfile
        
        # 4. 检查最终空间状态
        echo ">>> Final Disk Space Status:"
        df -h
        echo ">>> Final Memory/Swap Status:"
        free -h
        
    - name: Initialization Environment （编译环境初始化）
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo -E apt -yqq purge firefox
        sudo -E apt -yqq update
        sudo -E apt -yqq full-upgrade
        sudo -E apt -yqq autoremove --purge
        sudo -E apt -yqq clean
        sudo -E timedatectl set-timezone "$TZ"
        
        sudo apt -yqq install dos2unix libfuse-dev
        sudo bash -c 'bash <(curl -sL https://build-scripts.immortalwrt.org/init_build_environment.sh )'
        
        # 建立绝对路径基地
        sudo mkdir -p /workdir
        sudo chown $USER:$USER /workdir
        echo "初始磁盘空间(根目录):"
        df -hT /

    - name: Clone source code & Setup Base （克隆源码与标准化路径）
      run: |
        cd /workdir
        # 克隆 OpenWrt 到 /workdir/openwrt
        git clone --depth 1 $REPO_URL -b $REPO_BRANCH openwrt
        # 将当前仓库内的配置、脚本同步到源码目录中
        cp -r $GITHUB_WORKSPACE/. /workdir/openwrt/
        # 在工作区建立链接，确保后续步骤兼容 cd openwrt 指令
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

    # 4. 加载自定义 Feeds (diy-part1.sh)
    - name: Load custom feeds （加载自定义 Feeds）
      run: |
        [ -e $FEEDS_CONF ] && mv $FEEDS_CONF openwrt/feeds.conf.default
        chmod +x $DIY_P1_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P1_SH

    - name: Update feeds （更新插件源索引）
      run: cd openwrt && ./scripts/feeds update -a

    - name: Install feeds （安装插件软链接）
      run: cd openwrt && ./scripts/feeds install -a

    # 6. 加载自定义配置 (diy-part2.sh)
    - name: Load custom configuration （加载自定义配置diy-part2）
      run: |
        [ -e files ] && mv files openwrt/files
        [ -e $CONFIG_FILE ] && mv $CONFIG_FILE openwrt/.config
        chmod +x $DIY_P2_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P2_SH

    - name: SSH connection to Actions （SSH 远程调试）
      uses: mxschmitt/action-tmate@v3
      if: (github.event.inputs.ssh == 'true' && github.event.inputs.ssh  != 'false') || contains(github.event.action, 'ssh')

    - name: Download package （下载源码大包）
      run: |
        cd openwrt
        make defconfig
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    # 6. 加载自定义配置 (diy-part3.sh)
    - name: Load custom configuration （加载自定义配置diy-part3）
      run: |
        [ -e files ] && mv files openwrt/files
        [ -e $CONFIG_FILE ] && mv $CONFIG_FILE openwrt/.config
        chmod +x $DIY_P3_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P3_SH

    - name: Compile the firmware （固件正式编译）
      id: compile
      run: |
        cd openwrt
        echo ">>> 开始全火力编译..."
        df -hT /
        # 阶梯式策略：全核尝试 -> 失败单核重试 -> 失败输出详细日志
        make -j$(nproc) || make -j1 || make -j1 V=s
        
        echo "status=success" >> $GITHUB_OUTPUT
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

    - name: Upload bin directory （上传所有编译产物 IPK）
      uses: actions/upload-artifact@main
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true'
      with:
        name: OpenWrt_bin_${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: openwrt/bin

    - name: Organize files （整理固件发布包）
      id: organize
      if: env.UPLOAD_FIRMWARE == 'true' && !cancelled()
      run: |
        cd openwrt/bin/targets/*/*
        rm -rf packages
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Upload OpenWrt_firmware directory （上传固件镜像文件）
      uses: actions/upload-artifact@main
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        name: OpenWrt_firmware_${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: ${{ env.FIRMWARE }}

    - name: Generate release tag （生成发布标签）
      id: tag
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      run: |
        AUTHOR=$(echo "${{ env.REPO_URL }}" | cut -d'=' -f2 | awk -F'/' '{print $4}')
        [ -z "$AUTHOR" ] && AUTHOR="padavanonly"
        BRANCH="${{ env.REPO_BRANCH }}"
        BUILD_TIME=$(date +"%Y%m%d-%H%M")
        RELEASE_TAG="${{ env.DEVICE_NAME }}-${AUTHOR}-${BRANCH}-${BUILD_TIME}"
        RELEASE_TAG=$(echo "$RELEASE_TAG" | sed 's/[^a-zA-Z0-9._-]/-/g')
        echo "release_tag=${RELEASE_TAG}" >> $GITHUB_OUTPUT
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Upload firmware to release （发布固件至 Release）
      uses: softprops/action-gh-release@v2
      if: steps.tag.outputs.status == 'success' && !cancelled()
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.tag.outputs.release_tag }}
        body: |
          📱 设备名称: ${{ env.DEVICE_NAME }}
          🔗 源码地址: ${{ env.REPO_URL }}
          🌿 分支版本: ${{ env.REPO_BRANCH }}
          ⏰ 编译时间: $(date '+%Y-%m-%d %H:%M:%S')
        files: |
          ${{ env.FIRMWARE }}/*.bin
          ${{ env.FIRMWARE }}/*.img.gz
          ${{ env.FIRMWARE }}/*.manifest

    - name: Delete workflow runs （清理旧工作流历史）
      uses: GitRML/delete-workflow-runs@main
      with:
        retain_days: 1
        keep_minimum_runs: 10

    - name: Remove old Releases （清理旧 Release 发布）
      uses: dev-drprasad/delete-older-releases@v0.2.1
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      with:
        keep_latest: 10
        delete_tags: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
